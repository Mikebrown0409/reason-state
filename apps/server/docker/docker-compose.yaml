version: "3.9"
services:
  reason-state:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      # Uncomment to enable planner
      # - OPENAI_API_KEY=${OPENAI_API_KEY}
      # - OPENAI_MODEL=gpt-4o-mini
    restart: unless-stopped

